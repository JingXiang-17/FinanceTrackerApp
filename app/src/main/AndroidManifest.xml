<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".FinanceApp" 
        android:theme="@style/Theme.MyApp"
        android:allowBackup="true"
        android:label="Meowney Track"
        android:icon="@mipmap/meowneytrack"
        android:roundIcon="@mipmap/meowneytrack_round">

        <!-- The service must be INSIDE the application block -->
        <service
            android:name=".service.NotificationListener"
            android:label="Finance Tracker Auto-Logger"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Main Entry Point (Dashboard) -->
        <activity
            android:name=".ui.dashboard.DashboardActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other Activities -->
        <activity android:name=".ui.budget.BudgetActivity" android:exported="false" />
        <activity android:name=".ui.statistics.StatisticsActivity" android:exported="false" />
        <activity android:name=".ui.settings.SettingsActivity" android:exported="false" />

    </application>

</manifest>